/** OpenFreeMap Liberty — free vector tiles when the user opts in. */
export const ONLINE_STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty';

/** Short probe used to verify real reachability (not just navigator.onLine). */
export const ONLINE_PROBE_MS = 3000;
